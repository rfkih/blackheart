version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: blackheart-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: trading_db
    ports:
      - "5432:5432"
    volumes:
      - blackheart_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  blackheart_pgdata:
    external: true
