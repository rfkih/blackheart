config.stopBubbling = true
# Copy Spring's @Qualifier onto Lombok-generated constructor parameters so
# field-level `@Qualifier` on a final field injected via @RequiredArgsConstructor
# actually reaches the constructor (and therefore Spring's autowiring).
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
